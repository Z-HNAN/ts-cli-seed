# ts-cli-seed

基于typescript来构建包cli，加入jest测试，最终通过tsc编译,链接到本地

## 使用方式

> 建议使用npm,之后要使用`npm link`

```bash
# 初始化项目
npm install

# 运行测试环境
npm run test

# 构建项目
npm run build

# 连接到本地
npm link

# 直接在需要的目录使用
ts-cli-seed
```

## 编译方式

基于`tsc`直接编译

